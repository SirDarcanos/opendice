This project keeps a single source of truth for agent guidance in **`AGENTS.md`**.

👉 **Read [`AGENTS.md`](./AGENTS.md) before working in this codebase.** It covers what this
package is, the line it does not cross, why the randomness is load-bearing, and the traps
that have already caught someone.

Setup, code style, tests and how a release is cut live in
[`CONTRIBUTING.md`](./CONTRIBUTING.md).

Do not add separate guidance here — update `AGENTS.md` instead, so the two never drift
apart.
